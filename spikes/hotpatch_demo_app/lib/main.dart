import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      body: Center(
        child: Text('Engine OK', style: TextStyle(fontSize: 32)),
      ),
    ),
  ));
}
